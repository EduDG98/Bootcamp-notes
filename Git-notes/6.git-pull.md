# `git pull` 📲

- Descarga el contenido remoto en local y actualiza el repositorio local (_fetch_ + _merge_)
- Previamente, ir a la rama que se quiere descargar el contenido.
  - `git pull <remote>`
    - ### `git pull`
